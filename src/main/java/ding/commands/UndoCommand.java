package ding.commands;

import ding.TaskManager;

/**
 * Command placeholder for undo requests.
 */
public class UndoCommand extends Command {
    @Override
    public String execute(TaskManager taskManager) {
        return "";
    }
}
