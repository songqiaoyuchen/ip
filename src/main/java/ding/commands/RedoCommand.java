package ding.commands;

import ding.TaskManager;

/**
 * Command placeholder for redo requests.
 */
public class RedoCommand extends Command {
    @Override
    public String execute(TaskManager taskManager) {
        return "";
    }
}
